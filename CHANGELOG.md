# Changelog

모든 주요 변경사항은 이 파일에 기록됩니다.  
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.0.0/) 표준을 따릅니다.

---

## [0.4.2] - 2026-08-27

### 추가 (Added)
- **OS 파일 연결(File Associations) 및 우클릭/더블클릭 열기 지원**:
  - Windows/macOS에서 `.md`, `.markdown`, `.txt` 파일 우클릭 시 '연결 프로그램'으로 EveryMD 지원
  - 외부에서 파일 열기 시 단일 인스턴스(Single Instance) 유지 및 기존 창의 새 탭으로 자동 로드

---

## [0.4.1] - 2026-08-25

### 수정 (Fixed)
- **탐색기에서 `.txt` 파일 클릭 시 열리지 않음**: 파일 탐색기가 `.md`/`.markdown`만 허용하던 문제 수정 (열기 다이얼로그·드래그앤드롭은 `.txt`를 지원했으나 탐색기만 불일치)

---

## [0.4.0] - 2026-08-25

### 추가 (Added)
- **앱 자동 업데이트**: GitHub Releases 기반 업데이트 확인 및 설치 기능 추가 (별도 서버 없이 동작)
  - 설정 → 일반 탭에서 현재 버전 표시 및 "업데이트 확인" 가능
  - 새 버전 감지 시 릴리즈 노트 확인 후 다운로드(진행률 표시) → 설치 → 재시작으로 적용
  - 업데이트 아티팩트는 서명 키로 검증하여 변조된 배포 차단

### 변경 (Changed)
- package.json / tauri.conf.json / Cargo.toml 버전 동기화 (0.4.0)

---

## [0.3.1] - 2026-08-25

### 수정 (Fixed)
- **파일 이름 변경 시 데이터 유실**: 미저장 편집 내용이 있는 상태에서 이름 변경 시 변경 사항을 먼저 저장한 뒤 rename하도록 수정 (기존에는 디스크의 예전 내용이 저장된 것으로 표시되어 편집 내용 유실)
- **탐색기에서 이름 변경 시 열린 탭 경로 꼬임**: 파일명 변경 후 열려 있던 탭이 옛 경로를 가리켜 저장 시 중복 파일이 생기던 버그 수정 (열린 탭 경로/제목 동기 갱신)
- **드래그 앤 드롭 열기 실패**: Tauri v2에서 제거된 `file.path` 의존 제거, OS 네이티브 drag-drop 이벤트로 실제 파일 경로를 받아 열도록 수정 (드롭한 파일이 템플릿/상대경로로 열리던 문제)
- **Ctrl+스크롤 줌 간섭**: React passive wheel 리스너 문제로 페이지 전체 줌이 함께 실행되던 버그 수정 (non-passive 네이티브 리스너로 교체)
- **자동저장 누락**: 자동저장 대기 중 탭을 전환하면 저장이 취소되어 해당 탭이 계속 미저장으로 남던 버그 수정 (탭 전환/창 닫기 시 즉시 플러시)
- **창 닫기 시 데이터 유실 경고 없음**: 미저장 변경사항이 있을 때 창 닫기(타이틀바 X, Alt+F4) 확인창 추가
- **시작 크래시 방지**: localStorage 설정값(테마/단축키) 손상 시 앱이 실행되지 않던 문제 수정 (안전 파싱 적용)
- **열기 다이얼로그 필터**: `.txt` 파일도 열 수 있도록 필터 확장 (드래그앤드롭·README와 불일치 해소)

### 성능 (Performance)
- **워크스페이스 직렬화 최적화**: 타이핑 시마다 디스크 파일 전체 내용을 localStorage에 쓰던 구조 개선 — 디스크 파일은 경로만 저장하고 재시작 시 디스크에서 복원 (대용량 문서에서 입력 지연 및 저장 용량 초과 방지)

### 기타 (Chore)
- package.json / tauri.conf.json / Cargo.toml 버전 동기화

---

## [0.3.0] - 2026-08-09

### 수정 (Fixed)
- **코드블록 입력 불가 버그**: 코드 영역 클릭 후 글자 입력 시 에디터가 이동하고 입력이 되지 않던 버그 수정 (CodeMirror 포커스를 ProseMirror가 가로채던 문제)
- **Ctrl+스크롤 코드블록 간섭**: 코드블록 내부에서 Ctrl+스크롤 시 에디터 배율 대신 코드블록 자체 스크롤이 되도록 수정
- **H1/H2/H3 툴바 버튼**: 줄 복사 버그 → ProseMirror `setBlockType` API 직접 사용으로 완전 교체 (같은 레벨 재클릭 시 단락으로 토글)
- **코드블록 색상 가독성**: CodeMirror 전용 CSS 선택자로 다크/라이트 모드 모두 코드 가독성 개선, 구문 강조 색상 분리

### 변경 (Changed)
- **에디터 여백**: 좌우 동일한 5rem 여백으로 균등 조정
- **블록 핸들**: 텍스트와 겹치지 않도록 좌측 여백 안쪽(-68px)으로 위치 수정

---

## [0.2.0] - 2026-08-06

### 추가 (Added)
- **파일 드래그 앤 드롭 열기**: OS 파일탐색기에서 `.md`, `.txt`, `.markdown` 파일을 에디터 영역에 드롭하면 탭으로 즉시 열림
- **Ctrl+스크롤 배율 조절**: 에디터 위에서 `Ctrl+휠`로 글자 크기 조절 (12~32px)
- **폰트 변경 기능**: 설정 → 일반 탭에 에디터 글꼴 드롭다운 추가 (Inter, Noto Sans KR, JetBrains Mono 등)
- **코드 영역 색상 테마별 지원**: `--code-text` CSS 변수 추가, 커스텀 테마 편집기에도 코드 글자색 항목 추가
- **NSIS 자동 업그레이드**: 이전 버전이 설치된 상태에서 새 인스톨러 실행 시 자동으로 구버전 제거 후 재설치

### 변경 (Changed)
- **H1/H2/H3 툴바 버튼**: 텍스트 삽입 방식 → 현재 줄 전체를 해당 헤딩으로 변환하는 방식으로 개선
- **코드블록 툴바 버튼**: 이전에 무반응이던 버그 수정, paste event 기반으로 안정적으로 동작
- **코드 영역 가독성 개선**: 라이트/다크 모드 모두 코드 색상이 배경과 충분히 대비되도록 수정
- **블록 핸들(줄 설정창)**: 좌측 공간 여부와 관계없이 항상 좌측에 고정 표시

### 수정 (Fixed)
- 다크모드에서 코드 블록 배경/텍스트 색상이 라이트모드와 동일하게 표시되던 버그
- 툴바 `</>` 코드블록 버튼 클릭 시 아무 반응이 없던 버그

---

## [0.1.1] - 2026-07-15

### 추가 (Added)
- 사이드바 토글 단축키 변경 (`Ctrl+B` → `Ctrl+\`)
- 커스텀 테마 JSON 가져오기/내보내기 기능
- 테마 커스터마이저에 실시간 미리보기 패널 추가
- 새 창에서 독립 파일 열기 지원 (URL 쿼리 파라미터 방식)

### 변경 (Changed)
- 설정 모달 탭 구조 개편 (일반/단축키/테마 커스터마이저)
- One Dark Pro, Solarized Light 프리셋 테마 추가

---

## [0.1.0] - 2026-07-01

### 추가 (Added)
- 마크다운 WYSIWYG 에디터 (Milkdown/Crepe 기반)
- 탭 기반 다중 파일 편집
- 파일 탐색기 사이드바
- 라이트/다크 테마
- 자동 저장
- 단축키 편집기
- 상태바 (파일 경로, 폰트 크기, 테마 토글)
- Windows NSIS 인스톨러 패키지
